{
  "name": "playwright-enterprise-framework",
  "version": "1.1.0",
  "scripts": {
    "clean": "npx rimraf test-results playwright-report allure-results allure-report .tmp  reports npx rimraf session-state" ,
    "clean:session": "npx rimraf session-state",
    "clean:all": "npm run clean & npm run clean:session",
    "clean:reports": "npx rimraf reports",
    "pretest": "npm run clean",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:local": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"New-Item -ItemType Directory -Force .tmp | Out-Null; $env:TEMP = (Resolve-Path .tmp).Path; $env:TMP = $env:TEMP; npx playwright test\"",
    "smoke": "playwright test --grep @smoke",
    "regression": "playwright test --grep @regression",
    "report": "playwright show-report",
    "allure:generate": "allure generate allure-results --clean",
    "allure:open": "allure open"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "@types/nodemailer": "^6.4.23",
    "allure-playwright": "^2.0.0",
    "dotenv": "^16.4.0",
    "nodemailer": "^8.0.1",
    "winston": "^3.10.0",
    "xlsx": "^0.18.5"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
